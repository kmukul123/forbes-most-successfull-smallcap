feat: Implement swipe navigation using Hammer.js swipe gesture

- Changes gesture recognition from `pan` to `swipe` in `description-page.html` and `app.config.ts` for more intuitive navigation.
- Updates method names in `description-page.ts` from `onPanLeft/Right` to `onSwipeLeft/Right` to reflect the change in gesture type.
- Configures the `swipe` recognizer in `app.config.ts` to specifically detect horizontal swipes.