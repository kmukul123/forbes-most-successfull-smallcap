fix: Revert swipe sensitivity to Hammer.js default

- Removes the explicit `threshold` setting for pan gestures in `app.config.ts`, allowing Hammer.js to use its default sensitivity of 10 pixels. This should provide a more balanced swipe experience.